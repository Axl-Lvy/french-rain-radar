services:
  caddy:
    image: caddy:2-alpine
    container_name: french-rain-radar-caddy
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ../backend/caddy/Caddyfile.dev:/etc/caddy/Caddyfile:ro
      - ./data/tiles:/srv:ro
