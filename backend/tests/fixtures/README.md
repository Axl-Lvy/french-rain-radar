# Test fixtures

Drop a small representative `sample.grib2` here once you've successfully called the Météo-France API. Keep it under a few MB so the repository stays light. A typical radar-mosaic single-timestep GRIB2 is ~1–2 MB after subsetting.
